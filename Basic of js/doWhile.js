let num = 0;

do {
    console.log("hola");

} while (num !== 0);

while (num !== 0) {
    console.log("hola");
}

// NOTA: el primer ciclo se ejecuta al menos una vez, mientras que el segundo no se ejecuta si la condición es falsa desde el principio. Esto es una diferencia clave entre `do...while` y `while`.