// operadores de comparacion

// == Compara si dos valores son iguales
// === compara si tanto el tipo de dato como los valores son iguales
// != Compara si dos valores son diferentes
// !== Compara si tanto el tipo de dato como los valores son diferentes
// > Compara si el valor de la izquierda es mayor que el de la derecha
// < Compara si el valor de la izquierda es menor que el de la derecha
// >= Compara si el valor de la izquierda es mayor o igual que el de la derecha
// <= Compara si el valor de la izquierda es menor o igual que el de la derecha
const a = 10;
const b = 20;
const c = '10';
const d = 10;

console.log(a != c);  

// logicos
// && Compara si ambos valores son verdaderos
// || Compara si al menos uno de los valores es verdadero
// ! Invierte el valor de verdad de una expresión
console.log(a === d && b > a); // true
console.log(a === d || b < a); // true
console.log(!(a === d)); // false



